# nettrek — Current State

Human-readable context for lifecycle doc generation. The LLM reads this
for "what just shipped" and "active blockers" context — the sprint number
itself comes from `.sprint/sprint-number` (written automatically by sprint-merge.sh).

## What Just Shipped

*(Update after each sprint — describe what was built and merged)*

## Active Blockers

*(List anything blocking forward progress — tech debt, external dependencies, broken tests)*

## Platform Status

*(Brief health summary — what's live, what's not, known issues)*
