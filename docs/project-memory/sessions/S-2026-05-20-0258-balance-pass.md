# Session

Session-ID: S-2026-05-20-0258-balance-pass
Title: Game balance pass (F-004) — main gun curve
Date: 2026-05-20
Author: Claude (Opus 4.7) + Chad

## Goal

Begin F-004: fix the difficulty curve. First target — the main cannon DPS
cliff.

## Context

Balance had been tuned reactively (HP bumps, player-power HP scaling,
off-screen invulnerability). A holistic audit was needed.

## Changes Made — iteration 1

Audited effective main-gun DPS per level. Found a 3.1x cliff at L3→L4:
damage, shot count, and fire rate all scaled together and multiplied.

- Damage curve `1+floor(lvl/2)` = [1,2,2,3,3] → flat lookup [2,3,4,5,6].
- L4 shot count 5 → 4 (the 5th shot was part of the cliff).
- `getFireRate` floor 3 → 5: L4/L5 intervals [4,3] → [5,5], so fire rate
  stops being a third DPS multiplier at the top.

Result: L3→L4 DPS jump 3.1x → ~1.7x; L1 DPS roughly doubles (fixes the
fresh-ship-vs-cruiser slog).

## Decisions Made

- Smooth the player DPS curve first, then re-tune enemy HP scaling against
  it — one variable at a time so playtest feedback is attributable.

## Changes Made — iteration 2

Iteration 1 smoothed the curve *shape* but did not lower the top end (new
L5 ~360 all-hit DPS vs old ~300). The "too fast when armed up" complaint
needs the HP lever:

- `powerMult` (enemy HP vs player power) `1 + power*1.4` -> `1 + power*2.2`.
  Maxed-loadout HP multiplier 2.4x -> 3.2x.

Effect: maxed cruiser TTK ~0.75s -> ~1.0s; maxed elite ~1.3s -> ~1.8s.
Early game is unaffected (power ~0.1 -> multiplier 1.14 vs 1.22, negligible).

## Target TTK table (the curve we are tuning toward)

Time-to-kill should drop moderately as the player powers up — the player
should *feel* stronger — but never collapse to instant.

| Enemy   | Fresh ship | Maxed loadout |
|---------|-----------|---------------|
| Fighter | ~0.35s    | ~0.25s        |
| Bomber  | ~0.8s     | ~0.5s         |
| Turret  | ~1.0s     | ~0.6s         |
| Cruiser | ~3.0s     | ~1.0s         |
| Elite   | ~4s+      | ~1.8s         |

## Open Questions

- Playtest validation of iterations 1+2 still pending (user said "keep
  going" rather than reporting feel — these are math-driven).
- Per-stage difficulty-curve verification (F-004 criterion) — needs a
  playtest sweep across all 11 stages.
- Early-game elite TTK is long (~5s with an L1 gun) but elites rarely
  spawn early — left as-is unless playtest shows it matters.

## Links

Backlog:
- F-004 - Game balance pass
